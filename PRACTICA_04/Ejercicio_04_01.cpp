// Materia: Programación I, Paralelo 1
// Autor: Carlos Riveros Lopez.
// Fecha creación: 16/09/2024
// Número de ejercicio: 1
// Problema planteado: Escriba declaraciones de arreglo para lo siguiente: a. una lista de 100 voltajes de precisión doble b. una lista de 50 temperaturas de precisión doble c. una lista de 30 caracteres, cada uno representando un código d. una lista de 100 años n número entero e. una lista de 32 velocidades de precisión doble f. una lista de 1000 distancias de precisión doble g. una lista de 6 números de código enteros

#include <iostream>
//Introducimos la biblioteca vector para una mejor manera
#include <vector>
using namespace std;

int main () {
    vector<double> voltajes (100);
    vector<double> temperaturas (50);
    vector<string> caracteres (30);
    vector<int> anios;
    vector<double> velocidades (32);
    vector<double> distancias (1000);
    vector<int> codigos (6);
}
